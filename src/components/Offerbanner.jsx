import React from 'react'

function Offerbanner() {
    var date = new Date()
    var msg = '', percent = 0
    if(date.getMonth == 1) {
        if(date.getDate == 1) {

        }
    }

    return (
        <React.Fragment>Offerbanner</React.Fragment>
    )
}

export default Offerbanner